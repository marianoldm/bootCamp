server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/marianoldm/bootCamp.git
          searchPaths: config-data
          username: m.lorendelamo@gmail.com
          password: mla04cam%
  application:
    name: config-service
  security:
    user:
      name: root
      password: s3cr3t